apiVersion: apps/v1
kind: Deployment
metadata:
  name: deepseek-r1-qwen3-8b-neuron
  namespace: vllm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deepseek-r1-qwen3-8b-neuron
  template:
    metadata:
      labels:
        app: deepseek-r1-qwen3-8b-neuron
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      automountServiceAccountToken: false
      nodeSelector:
        {{{KARPENTER_PREFIX}}}/instance-family: inf2
        # node.kubernetes.io/instance-type: inf2.8xlarge 
        # karpenter.sh/capacity-type: on-demand
        {{#if compile}}
        node.kubernetes.io/instance-type: inf2.8xlarge
        karpenter.sh/capacity-type: on-demand
        {{else}}
        node.kubernetes.io/instance-type: inf2.xlarge
        {{/if}}
      containers:
        - name: vllm
          image: public.ecr.aws/agentic-ai-platforms-on-k8s/vllm-neuron:deepseek-r1-qwen3-8b
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - NET_RAW
            seccompProfile:
              type: RuntimeDefault
          command: ["vllm", "serve"]
          args:
            - /root/.cache/huggingface/hub/models--deepseek-ai--DeepSeek-R1-0528-Qwen3-8B/snapshots/6e8885a6ff5c1dc5201574c8fd700323f23c25fa
            - --served-model-name=deepseek-r1-qwen3-8b-neuron
            - --trust-remote-code
            # - --gpu-memory-utilization=0.90
            # - --max-model-len=32768 # 32K
            # DeepSeek-R1 specific
            - --reasoning-parser=deepseek_r1
            # Neuron specific
            - --tensor-parallel-size=2
            # - --gpu-memory-utilization=0.95
            - --max-num-seqs=1
            - --max-model-len=16384
          env:
            {{#if compile}}
            - name: HUGGING_FACE_HUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: hf-token
                  key: token
            {{/if}}
            - name: NEURON_COMPILED_ARTIFACTS
              value: /root/.cache/neuron/deepseek-ai/DeepSeek-R1-0528-Qwen3-8B
          ports:
            - name: http
              containerPort: 8000
          {{#if compile}}
          resources:
            requests:
              cpu: 24 #75%
              memory: 96Gi #75%
              aws.amazon.com/neuroncore: 2
            limits:
              aws.amazon.com/neuroncore: 2
          {{else}}
          resources:
            requests:
              cpu: 3 #75%
              memory: 12Gi #75%
              aws.amazon.com/neuroncore: 2
            limits:
              aws.amazon.com/neuroncore: 2
          {{/if}}
      {{#if compile}}
          volumeMounts:
            - name: huggingface-cache
              mountPath: /root/.cache/huggingface
            - name: neuron-cache
              mountPath: /root/.cache/neuron
      volumes:
        - name: huggingface-cache
          persistentVolumeClaim:
            claimName: huggingface-cache
        - name: neuron-cache
          persistentVolumeClaim:
            claimName: neuron-cache
      {{/if}}
      tolerations:
        - key: aws.amazon.com/neuron
          operator: Exists
          effect: NoSchedule
---
apiVersion: v1
kind: Service
metadata:
  name: deepseek-r1-qwen3-8b-neuron
  namespace: vllm
spec:
  selector:
    app: deepseek-r1-qwen3-8b-neuron
  ports:
    - name: http
      port: 8000
